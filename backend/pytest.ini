[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    unit: fast isolated unit tests
    integration: API and end-to-end flow tests
filterwarnings =
    ignore::DeprecationWarning
